import Breadcrumb from '../components/Breadcrumbs/Breadcrumb';
import DefaultLayout from '../layout/DefaultLayout';

const Calendar = () => {
  return (
    <DefaultLayout>
      <Breadcrumb pageName="Keuangan" />
    </DefaultLayout>
  );
};

export default Calendar;
